# 需求阶段评测

这个目录包含 `mdc-specify` 的评测 prompts。

## 目的

这些评测用于验证需求阶段是否真正做到：

- 先澄清再起草规格
- 不提前进入设计
- 正确把草稿交给 `mdc-review(review_type=spec)`

## 建议评分关注点

1. 是否主动澄清范围、约束、验收标准
2. 是否避免提前输出设计方案
3. 是否把结果交给 `mdc-review(review_type=spec)`
